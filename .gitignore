# dependencias y scratch (no forman parte del sitio estático)
node_modules/
.work/
.vercel/
.DS_Store
*.log

# tooling de QA local — el sitio desplegado es solo HTML + JS + assets
package.json
package-lock.json
